package eu.mosaico_project.shadow_sysml.impl;


import eu.mosaico_project.shadow_sysml.Relationship;
import org.omg.sysml.lang.sysml.Subsetting;

@Deprecated
public class SubsettingImpl implements Relationship {
    //final Feature subsetting, subsetted ;

    public SubsettingImpl(Subsetting st) {
        //this.subsetted = Simplifier.simplifyFeature(st.getSubsettedFeature());
        //this.subsetting = Simplifier.simplifyFeature(st.getSubsettingFeature());
    }
}
